apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gofr-task-api.fullname" . }}-test-connection"
  labels:
    {{- include "gofr-task-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gofr-task-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
